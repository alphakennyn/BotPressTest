import React from "react";
import logo from "../../logo.png";

const Loading = () => (
  <div id="loading-icon">
    <img src={logo} className="rotate" alt="loading-logo" />
  </div>
);

export default Loading;
